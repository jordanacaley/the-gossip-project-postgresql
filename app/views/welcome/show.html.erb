<h1>Welcome, <%= params[:first_name].capitalize() %>!</h1>

<p>This is our super gossip website. Cool, isn't it?</p>

<p>Here are all of our gossips:</p>

  <div class="row">
    <% Gossip.all.each do | gossip | %>
      <div class="card text-white bg-info m-3" style="width: 18rem;">
        <div class="card-body">
          <h5 class="card-title"><%= User.find_by(id: gossip.user_id).first_name %></h5>
          <p class="card-text"><%= gossip.title %></p>
          <span class="btn btn-light"><%= link_to "View", gossip_path(@id = gossip.id) %></span>
        </div>
      </div>
    <% end %>
  </div>

</body>